---
- name: Create config
  template:
    src: sshd_config.j2
    dest: /etc/ssh/sshd_config
    mode: "0664"
  notify: sshd_reload

- name: Add handler
  meta: flush_handlers

- name: Start sshd service
  service:
    name: sshd
    state: started
    enabled: true
